---
title: "R Notebook"
output: html_notebook
---

Download the Fsample ESTC data

```{r}

```

Turn the raw data into a network:

```{r}

```


Calculate some basic network metrics:

```{r}


```

Use the available data to make a subset of the full ESTC. Calculate group membership using `group_louvain()`. 

Turn the network into a dataframe using `as_tibble()`. Summarise the data per group, and calculate the `document_type` and `gatherings` information using `summarise()`. 

```{r}

```



